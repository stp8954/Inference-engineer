# Sources

One stub per ingested source, named `YYYY-MM-DD-short-slug.md`. Raw PDFs live in the Google Drive inbox folder (`inference-engineer-inbox`); web sources are referenced by URL. See ../WIKI_SCHEMA.md for the stub format.
